{
  "$schema": "../../pattern.schema.json",
  "name": "better-auth",
  "description": "better-auth v1 with the Drizzle adapter (Postgres). Email+password, email verification, optional Google OAuth, rate limiting and TanStack Start cookies. Ships auth tables, server/client instances, session helpers and a route guard.",
  "framework": "tanstack-start",
  "detect": {
    "deps": ["better-auth"],
    "files": []
  },
  "dependsOn": ["drizzle"],
  "integratesWith": ["trpc"],
  "capabilities": ["mailer", "email-kit"],
  "deps": ["better-auth"],
  "env": [
    "BETTER_AUTH_URL",
    "BETTER_AUTH_SECRET",
    "BETTER_AUTH_GOOGLE_CLIENT_ID",
    "BETTER_AUTH_GOOGLE_CLIENT_SECRET"
  ],
  "files": [
    { "from": "files/lib/roles.ts", "to": "<srcRoot>/lib/roles.ts" },
    { "from": "files/server/better-auth/config.ts", "to": "<srcRoot>/server/better-auth/config.ts" },
    { "from": "files/server/better-auth/client.ts", "to": "<srcRoot>/server/better-auth/client.ts" },
    { "from": "files/server/better-auth/server.ts", "to": "<srcRoot>/server/better-auth/server.ts" },
    { "from": "files/server/better-auth/session.ts", "to": "<srcRoot>/server/better-auth/session.ts" },
    { "from": "files/server/better-auth/emails.tsx", "to": "<srcRoot>/server/better-auth/emails.tsx" },
    { "from": "files/server/better-auth/index.ts", "to": "<srcRoot>/server/better-auth/index.ts" },
    { "from": "files/server/db/schemas/auth.schema.ts", "to": "<srcRoot>/server/db/schemas/auth.schema.ts" },
    { "from": "files/routes/api/auth/$.ts", "to": "<srcRoot>/routes/api/auth/$.ts" },
    { "from": "files/routes/_authed.tsx", "to": "<srcRoot>/routes/_authed.tsx" }
  ],
  "wiring": [
    "Append `export * from './auth.schema'` to <srcRoot>/server/db/schemas/index.ts (created by the drizzle pattern).",
    "emails.tsx imports ~/server/email (mailer capability) and ~/emails/{reset-password,verify-email} (email-kit). Ensure both capabilities are added; create the two templates if missing."
  ],
  "notes": [
    "Auth tables (user/session/account/verification) live in auth.schema.ts; better-auth manages them via the Drizzle adapter. The `role` field is a custom additionalField backed by ~/lib/roles.",
    "BETTER_AUTH_SECRET is read implicitly from env by better-auth; the rest are validated in env.ts."
  ]
}
