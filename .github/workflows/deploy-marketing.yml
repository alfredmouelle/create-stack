name: Deploy marketing Worker

on:
  workflow_dispatch:
    inputs:
      build_mode:
        description: Build mode to deploy to the isolated Worker
        required: true
        default: validation
        type: choice
        options:
          - validation
          - public
      worker_url:
        description: Full URL of the deployed Worker to verify after deployment
        required: true
        type: string

concurrency:
  group: deploy-marketing
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy marketing Worker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Build, smoke test, and deploy
        run: pnpm --filter @alfredmouelle/marketing deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          MARKETING_BUILD_MODE: ${{ inputs.build_mode }}
      - name: Verify deployed Worker
        run: pnpm --filter @alfredmouelle/marketing verify:deployment
        env:
          MARKETING_BUILD_MODE: ${{ inputs.build_mode }}
          MARKETING_DEPLOYMENT_URL: ${{ inputs.worker_url }}
