# Each capability is selected by a *_PROVIDER var; only the chosen adapter's keys
# are read. Defaults are dev-friendly (local / memory / console / noop).

# mailer — resend | brevo | ses
EMAIL_PROVIDER=resend
EMAIL_FROM=no-reply@example.com
RESEND_API_KEY=
BREVO_API_KEY=
# ses — also reads AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY (storage section)
AWS_REGION=
SES_CONFIGURATION_SET=

# storage — s3 | r2 | gcs | local
STORAGE_PROVIDER=local
STORAGE_LOCAL_DIR=.storage
STORAGE_PUBLIC_URL=http://localhost:3000/files
S3_BUCKET=
S3_REGION=
S3_ENDPOINT=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
GCS_BUCKET=
GOOGLE_CLOUD_PROJECT=

# jobs — inngest | trigger | memory
JOBS_PROVIDER=memory
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=
# trigger.dev — tasks deploy via the Trigger.dev CLI; the adapter only triggers
TRIGGER_SECRET_KEY=

# logger — pino | console
LOGGER_PROVIDER=console
LOG_LEVEL=info

# cache — redis | memory
CACHE_PROVIDER=memory
REDIS_URL=

# analytics — posthog | plausible | noop
ANALYTICS_PROVIDER=noop
POSTHOG_API_KEY=
POSTHOG_HOST=
PLAUSIBLE_DOMAIN=
PLAUSIBLE_API_HOST=

# error tracking — sentry | console
ERROR_TRACKING_PROVIDER=console
SENTRY_DSN=
SENTRY_ENVIRONMENT=
